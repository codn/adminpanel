<% if @model.has_route?(:edit) && can?(:update, @model) %>
  <% if ! defined? path %>
    <% path = [:move_to_better, member] %>
  <% end %>
  <%= link_to(
    content_tag(
      :i,
      nil,
      class: 'fa fa-chevron-up'
    ),
    path,
    method: :put,
    remote: true
  ) %>
<% end %>
