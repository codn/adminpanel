<% if @model.has_route?(:edit) && can?(:update, @model) %>
  <%= link_to(
    content_tag(
      :i,
      nil,
      class: 'fa fa-chevron-down'
    ),
    [:move_to_worst, member],
    method: :put,
    remote: true
  ) %>
<% end %>
