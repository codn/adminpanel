<% if @model.has_route?(:show) %>
  <%=
    link_to(
      content_tag(
        :i,
        nil,
        class: 'fa fa-search-plus'
      ),
      [resource],
      title: t("action.show")
    )
  %>
<% end %>
