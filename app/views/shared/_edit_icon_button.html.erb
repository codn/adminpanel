<%=
  link_to(
    content_tag(
      :i,
      nil,
      :class => 'fa fa-pencil'
    ),
    [:edit, resource],
    :title => t("action.update") + " #{resource_name}",
    data: { no_turbolink: true }
  )
%>
